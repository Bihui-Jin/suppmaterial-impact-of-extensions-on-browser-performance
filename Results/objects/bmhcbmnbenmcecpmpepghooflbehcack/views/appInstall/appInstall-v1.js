$(document).ready(() => {
  const appInstallText = document.querySelector('.AppInstallPopover__text');
  appInstallText.innerHTML = localize(`Download the App and <br/> use Premium features for free`);
});
