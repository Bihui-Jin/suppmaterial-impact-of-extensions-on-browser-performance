function AALogger(fileName, functionName, errorMessage) {

    try {
        //var msg = fileName + '.' + functionName + ', ' + errorMessage;
        //console.log(msg);
    }
    catch (e) {

    }
    //    var htmlDocument;
    //    if (navigator.userAgent.indexOf("Firefox") != -1)
    //        htmlDocument = content.document;
    //    else
    //        htmlDocument = document;

    //    var errorDiv = htmlDocument.getElementById('errorDiv');
    //    var msg = fileName + '.' + functionName + ', ' + errorMessage;
    //    if (errorDiv)
    //        errorDiv.innerHTML += "<br />" + msg;
    //    else
    //        htmlDocument.getElementsByTagName('body')[0].innerHTML += '<div id="errorDiv" style="display: block; position: fixed; width: 500px; height: 300px; overflow: scroll; background-color: white; border: solid 1px red; color: red;">' + msg + '</div>';
    //    if (WriteExceptionLog && sendResponseCallback) {
    // var logMessage = '<AALog>' + navigator.userAgent + ': ' + fileName + '.' + functionName + ', ' + errorMessage + '</AALog>';
    // if (navigator.appName != 'Microsoft Internet Explorer')
    // sendResponseCallback({ data: logMessage });
    // }
}
"";
